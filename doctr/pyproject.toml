[project]
name = "doctr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "python-doctr[contrib,html,torch,viz]>=0.10.0",
    "torch>=2.5.1",
]
